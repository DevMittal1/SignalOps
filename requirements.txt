# LiveKit Voice AI Agent Dependencies
# Install: pip install -r requirements.txt

# LiveKit Agents framework
livekit-agents[deepgram,openai,silero,turn-detector]>=0.12.0
livekit>=0.17.0

# Web server (Twilio webhook handler)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# HTTP client
httpx>=0.27.0

# Twilio (optional — for outbound call management)
twilio>=9.0.0

# Config & utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Monitoring
prometheus-client>=0.20.0   # optional: expose /metrics endpoint
